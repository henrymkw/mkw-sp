These are random notes I made when looking through ghidra and wfc-server, before making the design doc. Will remove this before merging to develop/main branch.

There are a few different cases matchmaking has to consider:

1. Private Rooms (Froom)
  a. Creating a Froom
  b. Joining a Froom
2. Public Rooms
  a. Joining via public lobby
    i. WorldWide: List of rooms
    ii. Regional: Each region has a list of rooms
  b. Joining via a friend

On the client side, we need to prevent DWC/Gamespy matchmaking code from running. The exact scope of these changes are unknown, here are some of the bigger ones:

Most of the logic is in DWC_ProcessFriendsMatch() when s_dwcControl->state == DWC_STATE_MATCHING, so returning in this case does a lot in preventing vanilla logic from running.

Replacing DWC_SetupGameServer() logic with our own room creating logic.
Note: The vanilla friend status logic should still be intact and shouldn't need to be modified.

Creating a private room:

The client will connect to ServerBrowser and send reliable messages to it to request a room creation.
With that in mind, we need to go from serverBrowser.handleMessage() to getting a group created, which happens in qr2/group.go

I think this would require degutting nearly the entirety of group.go, minus the groups var.

It would need to send data needed in order to get 

Big thing I need to figure out is how to get the client to listen for responses over tcp from the serverBrowser, so the client can update its matchMaking infos.

But once we get the client to listen for the room, handling shouldn't be too too bad in theory. To handle, we would find the DWC functions that end up setting the matchMakingInfo fields. Examples of these are:

DWC_GetNumConnectionHost() sets mmInfo.numConnectedColsoles
DWC_SetDirectConnectedAIDBitmap() sets mmInfo.directConnectedBitmap
DWC_GetGroupId() sets mmInfo.groupId

Most important of them being DWC_GetAidBitmap(), which sets mmInfo.availableAids, since that tells the game that there are players in the room (even if just myself), and that means that all the others can be set.

Those DWC functions would have to be replaced to return what the handler receives. So if the server sends over the aidbitmap, it should be stored somewhere, so that DWC_GetAidBitmap() can return it.

How I handle matchmaking suspension could be interesting too, since that is unanimous

Maybe I don't need to be careful about endianness anymore, which is noce for DWC_GetConnectionUserData()





